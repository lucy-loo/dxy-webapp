import * as React from 'react'
import * as globalStyle from '@/styles/index.module.css'

function RecomendList(): JSX.Element {
  return <div className={globalStyle.recomendList} />
}

export default RecomendList
